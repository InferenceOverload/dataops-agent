# DataOps Agent - Environment Configuration Template
# Copy this file to .env and fill in your values

# ============================================================================
# LLM Provider Configuration
# ============================================================================

# LLM Provider: 'anthropic' (direct API) or 'bedrock' (AWS Bedrock)
# - Use 'anthropic' for personal development with Anthropic API key
# - Use 'bedrock' for organizational deployment with AWS Bedrock
LLM_PROVIDER=anthropic

# Temperature setting for all LLM calls (0.0 to 1.0)
LLM_TEMPERATURE=0.7

# ============================================================================
# Anthropic API Configuration (when LLM_PROVIDER=anthropic)
# ============================================================================

# Anthropic API Key (required for direct Anthropic API access)
ANTHROPIC_API_KEY=your_anthropic_api_key_here

# Model name for Anthropic API (optional, defaults to claude-sonnet-4-20250514)
ANTHROPIC_MODEL=claude-sonnet-4-20250514

# ============================================================================
# AWS Bedrock Configuration (when LLM_PROVIDER=bedrock)
# ============================================================================

# Bedrock Model ID (optional, defaults to anthropic.claude-sonnet-4-20250514-v1:0)
BEDROCK_MODEL_ID=anthropic.claude-sonnet-4-20250514-v1:0

# Bedrock Region (optional, falls back to AWS_REGION)
BEDROCK_REGION=us-east-1

# ============================================================================
# AWS Configuration
# ============================================================================

# AWS Region (default: us-east-1)
AWS_REGION=us-east-1

# AWS Credentials (optional if using IAM roles or AWS CLI profiles)
# If running on EC2/ECS with IAM roles, these can be omitted
AWS_ACCESS_KEY_ID=your_aws_access_key_id
AWS_SECRET_ACCESS_KEY=your_aws_secret_access_key

# ============================================================================
# S3 Configuration
# ============================================================================

# Default S3 bucket for workflows
# Workflows can override this by passing bucket parameter to tools
AWS_S3_BUCKET=your-default-s3-bucket

# Example: If your JIL files are in s3://dataops-jil-files/
# Then set: AWS_S3_BUCKET=dataops-jil-files

# ============================================================================
# DynamoDB Configuration
# ============================================================================

# Default DynamoDB table for workflows
# Workflows can override this by passing table parameter to tools
AWS_DYNAMODB_TABLE=your-default-dynamodb-table

# Example: For workflow state tracking
# AWS_DYNAMODB_TABLE=dataops-workflow-state

# ============================================================================
# LangGraph Configuration (Optional)
# ============================================================================

# LangSmith tracing (optional - for debugging)
# LANGCHAIN_TRACING_V2=true
LANGSMITH_API_KEY=your_langsmith_api_key
# LANGCHAIN_PROJECT=dataops-agent