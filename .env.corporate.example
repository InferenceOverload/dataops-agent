# DataOps Agent - Corporate Environment Configuration Template
# This is a reference template for corporate deployments
# Copy this file to .env and adjust paths for your organization

# ============================================================================
# LLM Provider Configuration (Corporate Recommended)
# ============================================================================

# Use AWS Bedrock for organizational deployment
LLM_PROVIDER=bedrock
BEDROCK_MODEL_ID=anthropic.claude-sonnet-4-20250514-v1:0
BEDROCK_REGION=us-east-1
LLM_TEMPERATURE=0.7

# ============================================================================
# AWS Configuration
# ============================================================================

AWS_REGION=us-east-1

# AWS Credentials (optional if using IAM roles or AWS CLI profiles)
# Recommended: Use IAM roles on EC2/ECS instead of hardcoded credentials
# AWS_ACCESS_KEY_ID=your_aws_access_key_id
# AWS_SECRET_ACCESS_KEY=your_aws_secret_access_key

# S3 Configuration
AWS_S3_BUCKET=your-corporate-s3-bucket

# DynamoDB Configuration
AWS_DYNAMODB_TABLE=your-corporate-dynamodb-table

# ============================================================================
# Corporate SSL/TLS Certificate Configuration
# ============================================================================

# Path to corporate CA certificate bundle
# This handles SSL interception by corporate proxies
# Common paths:
# - Linux (Ubuntu/Debian): /etc/ssl/certs/ca-certificates.crt
# - Linux (RHEL/CentOS): /etc/pki/tls/certs/ca-bundle.crt
# - macOS: /etc/ssl/cert.pem
# - Windows: C:/Program Files/Git/mingw64/ssl/certs/ca-bundle.crt
# - Custom: /path/to/corporate/ca-bundle.crt

SSL_CERT_FILE=/etc/ssl/certs/ca-certificates.crt
REQUESTS_CA_BUNDLE=/etc/ssl/certs/ca-certificates.crt

# ============================================================================
# Corporate Proxy Configuration
# ============================================================================

# HTTP/HTTPS Proxy (if required)
# Replace with your corporate proxy server and port
HTTP_PROXY=http://proxy.company.com:8080
HTTPS_PROXY=http://proxy.company.com:8080

# Proxy with authentication (if required)
# HTTP_PROXY=http://username:password@proxy.company.com:8080
# HTTPS_PROXY=http://username:password@proxy.company.com:8080

# No proxy for local addresses
NO_PROXY=localhost,127.0.0.1,.company.com

# ============================================================================
# Corporate PyPI Mirror (Optional)
# ============================================================================

# If your organization uses an internal PyPI mirror
# PIP_INDEX_URL=https://pypi.company.com/simple
# PIP_TRUSTED_HOST=pypi.company.com

# ============================================================================
# LangGraph Configuration (Optional)
# ============================================================================

# LangSmith tracing (optional - for debugging)
# LANGCHAIN_TRACING_V2=true
# LANGSMITH_API_KEY=your_langsmith_api_key
# LANGCHAIN_PROJECT=dataops-agent-corporate

# ============================================================================
# Security Notes
# ============================================================================

# 1. This file should be added to .gitignore (already configured)
# 2. Never commit credentials to version control
# 3. Use IAM roles when possible instead of hardcoded AWS credentials
# 4. Use AWS Secrets Manager or Parameter Store for production secrets
# 5. Only disable SSL verification temporarily for debugging - never in production

# ============================================================================
# Quick Start for Corporate Environments
# ============================================================================

# 1. Copy this file: cp .env.corporate.example .env
# 2. Update paths and values above for your organization
# 3. Export environment variables: source .env (Linux/macOS)
# 4. Run setup: make corporate-setup
# 5. Test: uv sync
# 6. Deploy: langgraph dev

# For detailed setup instructions, see CORPORATE_SETUP.md
